# Installation

> **This guide is outdated.** Please use one of the following instead:
>
> - **Docker (recommended):** [docker/README.md](../docker/README.md)
> - **Bare-metal conda:** [installation/INSTALL_SLIM.md](INSTALL_SLIM.md)

The new slim installation removes DPVO and PyTorch3D, updates to PyTorch 2.1.2 + CUDA 11.8,
and uses `mmcv-full==1.7.2` via `openmim`. It runs on CUDA 11.8 through 12.x drivers.
